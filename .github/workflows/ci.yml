name: CI

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]
  workflow_dispatch: {}

permissions:
  contents: read
  packages: write
  pull-requests: read

jobs:
  # Detect whether the change touches any code-relevant path.
  # Docs-only changes (README, *.md outside the repo root data files,
  # docs/**) skip heavy work while every required check name still
  # reports success — this avoids the paths-ignore + required-status-checks
  # deadlock that previously blocked docs-only PRs.
  #
  # Data files bundled into the container image (tercet_missing_codes.csv,
  # docker-entrypoint.sh) ARE code-relevant — a change to either affects
  # what ships in `ghcr.io/.../:latest`, so they belong in the filter.
  #
  # On a `v*` tag push, dorny/paths-filter has no usable `base:` (the tag has
  # no prior ref to diff against) and can resolve `code` to `false`. Every
  # gate step below therefore also runs when `startsWith(github.ref,
  # 'refs/tags/v')`, so a tagged release always lints, tests and audits the
  # image it publishes rather than trusting a possibly-wrong filter result.
  changes:
    runs-on: ubuntu-latest
    outputs:
      code: ${{ steps.filter.outputs.code }}
    steps:
      - uses: actions/checkout@v7
      - uses: dorny/paths-filter@v4
        id: filter
        with:
          filters: |
            code:
              - 'app/**'
              - 'tests/**'
              - 'scripts/**'
              - 'requirements*.txt'
              - 'requirements.lock'
              - 'pyproject.toml'
              - 'Dockerfile'
              - 'Makefile'
              - 'docker-entrypoint.sh'
              - 'tercet_missing_codes.csv'
              - '.github/workflows/**'

  lint:
    needs: changes
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - if: needs.changes.outputs.code == 'true' || startsWith(github.ref, 'refs/tags/v')
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - if: needs.changes.outputs.code == 'true' || startsWith(github.ref, 'refs/tags/v')
        run: pip install ruff
      - if: needs.changes.outputs.code == 'true' || startsWith(github.ref, 'refs/tags/v')
        run: ruff check app/ scripts/
      - if: needs.changes.outputs.code == 'true' || startsWith(github.ref, 'refs/tags/v')
        run: ruff format --check app/ scripts/
      - if: needs.changes.outputs.code != 'true' && !startsWith(github.ref, 'refs/tags/v')
        run: echo "Docs-only change — skipping lint."

  import-check:
    needs: changes
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - if: needs.changes.outputs.code == 'true' || startsWith(github.ref, 'refs/tags/v')
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - if: needs.changes.outputs.code == 'true' || startsWith(github.ref, 'refs/tags/v')
        run: pip install -r requirements.txt
      - if: needs.changes.outputs.code == 'true' || startsWith(github.ref, 'refs/tags/v')
        run: python -c "from app.main import app; print('imports OK')"
      - if: needs.changes.outputs.code != 'true' && !startsWith(github.ref, 'refs/tags/v')
        run: echo "Docs-only change — skipping import-check."

  security:
    needs: changes
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - if: needs.changes.outputs.code == 'true' || startsWith(github.ref, 'refs/tags/v')
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - if: needs.changes.outputs.code == 'true' || startsWith(github.ref, 'refs/tags/v')
        run: pip install -r requirements.txt
      - if: needs.changes.outputs.code == 'true' || startsWith(github.ref, 'refs/tags/v')
        name: Dependency audit
        run: pip install pip-audit && pip-audit -r requirements.lock
      - if: needs.changes.outputs.code == 'true' || startsWith(github.ref, 'refs/tags/v')
        name: Static security analysis
        run: pip install bandit && bandit -r app/ scripts/ -c pyproject.toml
      - if: needs.changes.outputs.code != 'true' && !startsWith(github.ref, 'refs/tags/v')
        run: echo "Docs-only change — skipping security."

  test:
    needs: changes
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - if: needs.changes.outputs.code == 'true' || startsWith(github.ref, 'refs/tags/v')
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - if: needs.changes.outputs.code == 'true' || startsWith(github.ref, 'refs/tags/v')
        run: pip install -r requirements-dev.txt
      - if: needs.changes.outputs.code == 'true' || startsWith(github.ref, 'refs/tags/v')
        run: pytest tests/ -v
      - if: needs.changes.outputs.code != 'true' && !startsWith(github.ref, 'refs/tags/v')
        run: echo "Docs-only change — skipping tests."

  docker:
    needs: changes
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - if: needs.changes.outputs.code == 'true' || startsWith(github.ref, 'refs/tags/v')
        name: Build image
        run: docker build -t postalcode2nuts .
      - if: needs.changes.outputs.code == 'true' || startsWith(github.ref, 'refs/tags/v')
        name: Verify non-root user
        run: |
          user=$(docker run --rm postalcode2nuts whoami)
          echo "Container user: $user"
          [ "$user" = "appuser" ] || exit 1
      - if: needs.changes.outputs.code != 'true' && !startsWith(github.ref, 'refs/tags/v')
        run: echo "Docs-only change — skipping docker build."

  publish:
    # workflow_dispatch on main always publishes (the whole point of a manual
    # trigger); push to main publishes only if a code-relevant file changed.
    if: (github.ref == 'refs/heads/main' && (github.event_name == 'workflow_dispatch' || (github.event_name == 'push' && needs.changes.outputs.code == 'true'))) || startsWith(github.ref, 'refs/tags/v')
    needs: [changes, lint, import-check, test, security, docker]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/metadata-action@v6
        id: meta
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}
      - uses: docker/build-push-action@v7
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
